# Codeigniter-CRUD-basic
for routing, use application/config/routes.php (not .htacces)

database:
name "db_crud"
username "root" and no password.
table : "tb_barang"

bootstrap tweak can be found in folder assets.
